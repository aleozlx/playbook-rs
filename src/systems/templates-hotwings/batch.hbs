---
apiVersion: batch/v1
kind: Job
metadata:
  generateName: test-job-
spec:
  template:
    metadata:
      name: test_job
    spec:
      containers:
        - name: test
          image: {{ data.image.Str }}
          command: ["hostname"]
      restartPolicy: Never