---
apiVersion: batch/v1
kind: Job
metadata:
  generateName: batch-
  namespace: bluecheese
spec:
  template:
    metadata:
      name: bluecheese
    spec:
      containers:
        - name: step
          image: aleozlx/playbook-test:test1
          command: ["hostname"]
      restartPolicy: Never