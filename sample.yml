docker:
  image: aleozlx/tkstack2:latest
  runtime: nvidia
  gui: False
steps:
  - action: sys_exit
  # - name: Show GPU status
  #   action: nvidia_smi
  # - name: Shell inside the default docker environment.
  #   action: sys_shell
